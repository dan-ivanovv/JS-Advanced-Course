function strLength(one, two, three) {

    let total = one.length + two.length + three.length;
    let avg = Math.floor(total / 3);

    console.log(total);
    console.log(avg);
}
strLength('pasta', '5', '22.3')